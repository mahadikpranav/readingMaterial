class Book { 
  constructor(title, isbn, author,description) { 
     this.title = title; 
     this.isbn = isbn; 
     this.author = author; 
     this.description = description; 
  } 
}

module.exports = Book;